[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gpt-trainer"
version = "0.1.0"
description = "Transcript pipeline for custom GPT training (to be recreated)"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["gpt_trainer"]
